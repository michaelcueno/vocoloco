# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /live                       controllers.Application.stream
GET     /live1                      controllers.Application.stream1(room, user)
GET     /say                        controllers.Application.say(message)
GET     /say1                       controllers.Application.say1(room, message)
GET     /room                       controllers.Application.chatRoom
GET     /room1						controllers.Application.chatRoom1(room,user)
GET     /rooms                      controllers.Application.rooms

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
